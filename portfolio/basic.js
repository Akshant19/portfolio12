// const elements = document.querySelectorAll('.heading');
// elements.forEach(element => {
//   element.addEventListener('mouseover', () => changeText(element));
//   element.addEventListener('mouseout', () => resetText(element));
// });

// function changeText(element) {
//   element.innerHTML = 'Ashish Hiranwal';
// }

// function resetText(element) {
//   element.innerHTML = 'Portfolio';
// }
// let id = document.getElementById('heading1');
    function changeText() {
        document.getElementById('heading1').innerHTML ='Akshant.';
      }
      function resetText() {
        document.getElementById('heading1').innerHTML = 'Portfolio';
      }